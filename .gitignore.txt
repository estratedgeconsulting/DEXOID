# Save this file as  .gitignore  (with the leading dot, no .txt) in your
# project root, next to package.json.

node_modules
dist
dist-ssr
*.local

# logs
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# editors / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# environment — never commit secrets
.env
.env.*
!.env.example
